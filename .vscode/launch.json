{
  "version": "0.2.0",
  "configurations": [
    {
      "cwd": "${workspaceFolder}/vulnerable-application",
      "command": "npm start",
      "name": "Run npm start",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
